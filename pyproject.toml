[project]
name = "fly-in"
version = "0.1.0"
description = "Multi-agent pathfinding simulator."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flake8>=7.3.0",
    "mypy>=1.0",
]
